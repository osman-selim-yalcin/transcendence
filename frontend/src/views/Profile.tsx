import UserInfo from "../components/UserInfo.tsx"

export default function Profile() {

  return (
    <div>
      <h1>Profile</h1>
      <UserInfo />
      This one will be more detailed than the one in homepage
    </div>
  )
}
